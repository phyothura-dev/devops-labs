services:
  app:
    image: react-app
    build: .
    container_name: react-app
    ports:
      - "5173:5173"
    # environment:
    #   - CHOKIDAR_USEPOLLING=true
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host
